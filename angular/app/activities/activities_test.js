'use strict';

describe('myApp.activities module', function() {

  beforeEach(module('myApp.activities'));

  describe('activities controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var activitiesCtrl = $controller('activitiesCtrl');
      expect(activitiesCtrl).toBeDefined();
    }));

  });
});